{{- /* Lookup OIDC integration secret to get URLs */ -}}
{{- $oidcSecretObj := (lookup "v1" "Secret" .Values.konflux.tsf.namespace "tsf-trustificationauth-integration") | default dict -}}
{{- $secretData := required (printf "Secret 'tsf-trustificationauth-integration' not found in namespace '%s'" .Values.konflux.tsf.namespace) (get $oidcSecretObj "data") | default dict -}}
{{- $trustifyOIDCIssuerUrl := required (printf "oidc_issuer_url not found in secret 'tsf-trustificationauth-integration' in namespace '%s'" .Values.konflux.tsf.namespace) (get $secretData "oidc_issuer_url") | b64dec | toString -}}

{{- /* Lookup Trusted Artifact Signer integration secret to get URLs */ -}}
{{- $tasSecretObj := (lookup "v1" "Secret" .Values.konflux.tsf.namespace "tsf-tas-integration") | default dict -}}
{{- $secretData := required (printf "Secret 'tsf-tas-integration' not found in namespace '%s'" .Values.konflux.tsf.namespace) (get $tasSecretObj "data") | default dict -}}
{{- $fulcioInternalUrl := required (printf "fulcio_service_url not found in secret 'tsf-tas-integration' in namespace '%s'" .Values.konflux.tsf.namespace) (get $secretData "fulcio_service_url") | b64dec | toString -}}
{{- $fulcioExternalUrl := required (printf "fulcio_url not found in secret 'tsf-tas-integration' in namespace '%s'" .Values.konflux.tsf.namespace) (get $secretData "fulcio_url") | b64dec | toString -}}
{{- $rekorInternalUrl := required (printf "rekor_service_url not found in secret 'tsf-tas-integration' in namespace '%s'" .Values.konflux.tsf.namespace) (get $secretData "rekor_service_url") | b64dec | toString -}}
{{- $rekorExternalUrl := required (printf "rekor_url not found in secret 'tsf-tas-integration' in namespace '%s'" .Values.konflux.tsf.namespace) (get $secretData "rekor_url") | b64dec | toString -}}
{{- $tufInternalUrl := required (printf "tuf_service_url not found in secret 'tsf-tas-integration' in namespace '%s'" .Values.konflux.tsf.namespace) (get $secretData "tuf_service_url") | b64dec | toString -}}
{{- $tufExternalUrl := required (printf "tuf_url not found in secret 'tsf-tas-integration' in namespace '%s'" .Values.konflux.tsf.namespace) (get $secretData "tuf_url") | b64dec | toString -}}

{{- /* Lookup Trusted Profile Analyzer integration secret to get URLs */ -}}
{{- $tpaSecretObj := (lookup "v1" "Secret" .Values.konflux.tsf.namespace "tsf-trustification-integration") | default dict -}}
{{- $secretData := required (printf "Secret 'tsf-trustification-integration' not found in namespace '%s'" .Values.konflux.tsf.namespace) (get $tpaSecretObj "data") | default dict -}}
{{- $trustifyServiceUrl := required (printf "bombastic_api_service_url not found in secret 'tsf-trustification-integration' in namespace '%s'" .Values.konflux.tsf.namespace) (get $secretData "bombastic_api_service_url") | b64dec | toString -}}
{{- $trustifyUrl := required (printf "bombastic_api_url not found in secret 'tsf-trustification-integration' in namespace '%s'" .Values.konflux.tsf.namespace) (get $secretData "bombastic_api_url") | b64dec | toString -}}

{{- /* Lookup default OIDC issuer. Fallback to kubernetes.default.svc */ -}}
{{- $issuer := "https://kubernetes.default.svc" -}}
{{- $auth := lookup "config.openshift.io/v1" "Authentication" "" "cluster" -}}
{{- if $auth -}}
  {{- /* Safely dig into the spec to find the serviceAccountIssuer */ -}}
  {{- if and $auth.spec (hasKey $auth.spec "serviceAccountIssuer") (ne $auth.spec.serviceAccountIssuer "") -}}
    {{- $issuer = $auth.spec.serviceAccountIssuer -}}
  {{- end -}}
{{- end -}}

---
apiVersion: konflux.konflux-ci.dev/v1alpha1
kind: Konflux
metadata:
  name: konflux
  namespace: {{ .Values.konflux.namespace }}
spec:
  imageController:
    enabled: true
  telemetry:
    enabled: true
  info:
    spec:
      clusterConfig:
        data:
          enableKeylessSigning: true
          defaultOIDCIssuer: {{ $issuer }}
          buildIdentityRegexp: "^https://kubernetes.io/namespaces/[a-z0-9-]+-tenant/serviceaccounts/build-pipeline-[a-z0-9-]+$"
          tektonChainsIdentity: https://kubernetes.io/namespaces/openshift-pipelines/serviceaccounts/tekton-chains-controller
          fulcioInternalUrl: {{ $fulcioInternalUrl | quote }}
          fulcioExternalUrl: {{ $fulcioExternalUrl | quote }}
          rekorInternalUrl: {{ $rekorInternalUrl | quote }}
          rekorExternalUrl: {{ $rekorExternalUrl | quote }}
          tufInternalUrl: {{ $tufInternalUrl | quote }}
          tufExternalUrl: {{ $tufExternalUrl | quote }}
          trustifyOIDCIssuerUrl: {{ $trustifyOIDCIssuerUrl | quote }}
          trustifyServerInternalUrl: {{ $trustifyServiceUrl | quote }}
          trustifyServerExternalUrl: {{ $trustifyUrl | quote }}
      publicInfo:
        integrations:
{{- $ghSecretObj := (lookup "v1" "Secret" .Values.konflux.tsf.namespace "tsf-github-integration") | default dict -}}
{{- $secretData := (get $ghSecretObj "data") | default dict -}}
{{- if $secretData }}
          github:
            application_url: {{ get $secretData "htmlURL" | b64dec | toString | quote }}
{{- end }}
          sbom_server:
            url: "{{ $trustifyUrl }}/sbom/content/<PLACEHOLDER>"
            sbom_sha: "{{ $trustifyUrl }}/sboms/<PLACEHOLDER>"
  buildService:
    spec:
      pipelineConfig:
        defaultPipelineName: docker-build-oci-ta-min
        pipelines:
        - name: docker-build-oci-ta-min
          bundle: quay.io/rhpds/pipeline-docker-build-lab@sha256:20954ff6b57d0b1cbd0df5684605335eb715a041d7efc640f0fd77b160053752
          description: Lab-optimized pipeline with tenant resource limits (CPU 300m/500m, Memory 512Mi/1Gi)
  integrationService:
    spec:
      integrationControllerManager:
        manager:
          env:
          - name: DEFAULT_INTEGRATION_RESOLVER
            value: git
          - name: DEFAULT_INTEGRATION_URL
            value: https://github.com/rhpds/konflux-tssc-lab.git
          - name: DEFAULT_INTEGRATION_REVISION
            value: main
          - name: DEFAULT_INTEGRATION_PATH
            value: tekton/pipelines/enterprise-contract-low-resources.yaml
